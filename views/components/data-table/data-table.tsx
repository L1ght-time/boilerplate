type DataTableProps = {
  data: any;
  columns: any;
};

export const DataTable = (props: DataTableProps) => {
  const {} = props;
  //   const table = useReactTable({ data: [{}] });

  return <div>DataTable</div>;
};
